{% extends "themes/base_admin.html.twig" %}

{% block title %}Modification des rôles{% endblock %}

{% block description %}Modification des rôles{% endblock %}

{% block main %}
    <h1 class="text-center my-3 display-5">Modification des rôles</h1>

     <div class="container my-3">
        <div class="row">
            <div class="col-md-9 col-lg-7 mx-auto p-4 bg-white shadow">
                {{ form_start(form) }}

                    <div class="row">
                        <div class="col-md-12">
                            <div class="mb-3">
                                <div class="text-danger">{{ form_errors(form.roles) }}</div>
                                <label for="registration_form_email">Les rôles</label>
                                {{ form_widget(form.roles, {'attr': {'class': 'form-control'}}) }}
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12">
                                <div class="mb-3">
                                    <input type="submit" class="btn btn-primary" value="Modifier" formnovalidate />
                                </div>
                            </div>
                        </div>
                    </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}