<!DOCTYPE html>
<html lang="fr">
    <head>

        {# Début : Activation de l'encodage unicode des caractères #}
        <meta charset="UTF-8">
        {# Fin   : Activation de l'encodage unicode des caractères #}

        {# Début : Activation de la compatibilté avec Internet Explorer #}
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        {# Fin   : Activation de la compatibilté avec Internet Explorer #}

        {# Début : Minimum de responsive design à mettre en place #}
            {# L'affichage en largeur du site doit s'adapter à la largeur de l'écran sur lequel il est affiché #}
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        {# Fin   : Minimum de responsive design à mettre en place #}

        {# Début : Affichage du titre de la page dans l'onglet #}
        <title>Le blog de Pascal Monnier - {% block title %}{% endblock %}</title>
        {# Fin   : Affichage du titre de la page dans l'onglet #}

        {# Début : Chargement de la favicon #}
        <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('favicon/apple-touch-icon.png') }}">
        <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('favicon/avicon-32x32.png') }}">
        <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('favicon/favicon-16x16.png') }}">
        <link rel="manifest" href="{{ asset('favicon/site.webmanifest') }}">
        <link rel="mask-icon" href="{{ asset('favicon/safari-pinned-tab.svg') }}" color="#5bbad5">
        <meta name="msapplication-TileColor" content="#da532c">
        <meta name="theme-color" content="#ffffff">
        {# Fin   : Chargement de la favicon #}

        {# debut mise en place du chargement des balises meta importantes au SEO #}
        <meta name="robots" content="index, allow , all"/>
        <meta name="description" content="{% block description %} {% endblock %}"/>
        <meta name="keywords" content="dwwm, pascal monnier, symfony, PHP"/>
        {# fin mise en place du chargement des balises meta importantes au SEO #}


        {# Début : Chargement de la font family : poppins #}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">
        {# Fin   : Chargement de la font family : poppins #}
        
        {# Début : Chargement du css avec webpackencore #}
        {% block webpack_stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        {# Fin   : Chargement du css avec webpackencore #}

        {# Début : Chargement du js avec webpackencore #}
        {% block webpack_javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        {# Fin   : Chargement du js avec webpackencore #}

        {# Font awesome #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w==" crossorigin="anonymous" referrerpolicy="no-referrer" />

       
       

    </head>

    <body class = "bg-light">
    {#  barre de navigation #}
       {% include "layouts/nav/nav_visitor.html.twig"%}
       {# fin barre de navigation #}

        {# Début : Mise en place de la section d'introduction #}
    {% block header %}{% endblock %}
    {# Fin   : Mise en place de la section d'introduction #}


       {# debut : chargement du contenu specifique a chaque page #}
       <main class="container-fluid">
        {% block main %} {% endblock %}
    </main>
    {# fin : chargement du contenu specifique a chaque page #}

    {# Début: Chargement du pieds de page #}
    {% include "layouts/footer/foot_visitor.html.twig" %}
    {# Fin  : Chargement du pieds de page #}

    {# vanilla Js #}
    {% block js %}{% endblock %}

    
    </body>
</html>